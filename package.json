{
  "name": "monacapro-trading-platform",
  "version": "1.0.0",
  "description": "MonaCap Pro Trading Platform - Full Stack Application",
  "private": true,
  "engines": {
    "node": "20.x"
  },
  "scripts": {
    "install-all": "npm install && cd frontend && npm install --legacy-peer-deps && cd ../backend && npm install",
    "build": "cd frontend && npm run build",
    "dev": "concurrently \"cd frontend && npm start\" \"cd backend && npm start\"",
    "start": "cd frontend && npm start",
    "backend": "cd backend && npm start",
    "frontend": "cd frontend && npm start"
  },
  "devDependencies": {
    "concurrently": "^8.2.2"
  }
}
